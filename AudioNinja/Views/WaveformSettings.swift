import SwiftUI

/// Display preferences that outlive a document window.
enum WaveformSettings {
    /// Show one lane per channel instead of a single combined waveform.
    static let showsSeparateChannelsKey = "waveform.showsSeparateChannels"
}
